This folder contains the core depth map generation stack. It includes the input data (stereo_set) from the CARLA Simulator.

My_Depth_Map_Generator.ipynb is the notebook that implements the depth map generation logic from a pair of input stereo images. It is broadly split into 3 sections - Setup, DepthEstimation & Depth Map Generation and finally 
Estimating Collision to object. 
